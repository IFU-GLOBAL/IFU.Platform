version: 1
applications:
  - appRoot: ifu-static-site
    frontend:
      phases:
        build:
          commands:
            - echo "Using committed dist folder"
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'